#include <stdio.h>

/**
 * natural - prints sum of all the multiples of 3 or 5 below 1024
 *
 * Return: void
 */

void natural(void)
{
	int i, sum = 0;

	while (i < 1024)
	{
		if (i % 3 == 0 || i % 5 == 0)
			sum += i;
		i++;
	}
	printf("%d", sum);
}
