#include "main.h"

/**
 * _islower - checks for a lowercase character
 * @c: the character to check
 * Return: If lowercase 1, otherwise 0
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
